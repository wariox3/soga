login:
    pattern:   /login
    defaults:  { _controller: SogaSeguridadBundle:Seguridad:login }
    
login_check:
    pattern:   /login_check

logout:
    pattern:   /logout

registro:
    pattern:   /registro
    defaults:  { _controller: SogaSeguridadBundle:Seguridad:registro }